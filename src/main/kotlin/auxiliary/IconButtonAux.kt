package auxiliary

import androidx.compose.material.Icon
import androidx.compose.material.icons.Icons
import androidx.compose.material.icons.filled.CheckCircle
import androidx.compose.runtime.getValue
import androidx.compose.runtime.mutableStateOf
import androidx.compose.runtime.remember
import androidx.compose.runtime.setValue
import androidx.compose.ui.graphics.Color

class IconButtonAux (offsetX: Int, offsetY:Int) {
    /*var i = 1
    var offsetX = 0
    var offsetY = 0
    var color by remember { mutableStateOf(Color.Yellow) }
    IconButton(onClick = { defineFun(cardList!!,i) }, modifier = Modifier.offset(offsetX.dp,offsetY.dp)) {
        Icon(
            Icons.Default.CheckCircle ,contentDescription = "Localized description",
            tint = colorCard(cardList!!,i))
    }*/
}